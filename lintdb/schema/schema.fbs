namespace lintdb;

table Code {
    codes: [uint8];
}

// flatbuffers use uint32 internally and are limited to 2gb in size.
table InvertedDocument {
    doc_id:string;
    num_tokens:uint32;
    codes:[uint8];
    residuals:[float];
}

root_type InvertedDocument;